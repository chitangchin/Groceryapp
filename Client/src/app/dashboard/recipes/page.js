import React from 'react'
import recipeCards from '@/Components/recipeCards'

const Recipes = () => {
  return (
    <div>Recipes
      {/* Get request to recipes available */}
      <div>
        {/* Render all data into cards component from componenets folder */}
      </div>
    </div>
  )
}

export default Recipes